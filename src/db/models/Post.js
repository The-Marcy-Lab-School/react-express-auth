const knex = require('../knex');
// const { hashPassword, isValidPassword } = require('../../utils/auth-utils');

class Post{
    static async create() {
        try {
            
        }
        catch(error) {
            console.log('ERROR!') 
            return null;
        } 
    } 
}

module.exports = Post;