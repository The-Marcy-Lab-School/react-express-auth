export default function SusuMembers({ users }) {
    // return <li >{users.username} {users.make_payments = true ? '[]' : '[X]]'}</li>;
    return (
    <>
         {users.username} {users.make_payments = true ? '[]' : '[X]]'};
    </>
  )}
  